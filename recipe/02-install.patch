--- tvips/CMakeLists.txt.orig
+++ tvips/CMakeLists.txt
@@ -903,6 +903,7 @@ if(BUILD_PYTHON_MODULE)
   install(
     TARGETS
       pysmv
+    COMPONENT module
     DESTINATION "${PYTHON_SITE_PACKAGES}")
 endif()
 
